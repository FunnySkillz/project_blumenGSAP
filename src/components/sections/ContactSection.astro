---
import { contactDetails } from "../../data/site";
---

<section class="contact" id="kontakt" aria-labelledby="contact-title">
  <div class="contact__heading" data-reveal>
    <p class="eyebrow">Kontakt</p>
    <h2 id="contact-title">{contactDetails.name}</h2>
  </div>
  <div class="contact__proof" data-reveal>
    <span>
      <strong>Raimundstraße 39</strong>
      <small>Direkt in Linz</small>
    </span>
    <span>
      <strong>Persönlich gebunden</strong>
      <small>Für Anlass und Mensch</small>
    </span>
    <span>
      <strong>Schneller Kontakt</strong>
      <small>Anruf, Mail oder Route</small>
    </span>
  </div>
  <div class="contact-actions" data-reveal>
    <a class="contact-action contact-action--primary" href={contactDetails.phoneHref}>
      Jetzt anrufen
    </a>
    <a class="contact-action" href={contactDetails.mapsHref}>
      Route planen
    </a>
  </div>
  <div class="contact__grid">
    <a class="contact-tile" href={contactDetails.mapsHref} data-reveal>
      <span>Adresse</span>
      <strong>{contactDetails.addressLines[0]}<br />{contactDetails.addressLines[1]}</strong>
    </a>
    <a class="contact-tile" href={contactDetails.phoneHref} data-reveal>
      <span>Telefon</span>
      <strong>{contactDetails.phone}</strong>
    </a>
    <a class="contact-tile" href={contactDetails.emailHref} data-reveal>
      <span>E-Mail</span>
      <strong>{contactDetails.email}</strong>
    </a>
  </div>
</section>
